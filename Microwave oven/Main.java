#include<iostream>
using namespace std;
int main()
{
  float a,b,c;
  std::cin>>a>>b;
  c=(b*0.50);
  if(a==2)
    std::cout<<(b+c);
  else if(a==3)
    std::cout<<(b*2);
  else
    std::cout<<"Number of items is more";

}
  